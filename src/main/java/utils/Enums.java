package utils;

public class Enums {
    public enum OutputFormat {
        TXT,
        PRINT_ONLY,
    }
    public enum ElementType {
        Action,
        MultiPartGoal,
        SinglePartGoal
    }
}
