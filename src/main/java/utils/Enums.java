package utils;

public class Enums {
    public enum OutputFormat {
        TXT,
        PRINT_ONLY,
    }
    public enum GoalType {
        Action,
        MultiPartGoal,
        SinglePartGoal
    }
}
