package utils.enums;

/**
 * This is the enum that indicates the type of elements. It can be either an Action, a Multi Part Goal, or a Single Part Goal
 */
public enum ElementType {
    ACTION,
    MULTI_PART_CONDITION,
    SINGLE_PART_CONDITION
}
